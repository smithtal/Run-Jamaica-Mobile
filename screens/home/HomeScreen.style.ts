import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  greeting: {
    fontSize: 28,
  },
  welcome: {
    fontSize: 18,
  },
  header: {
    justifyContent: 'space-between',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 40,
  },
});
